using System;
using System.Collections.Generic;
using System.Data.Entity;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using Venus.Data;
using Venus.Model;
using Venus.Repository;

namespace Venus.ConsoleApplication
{
    class Program
    {
        static void Main(string[] args)
        {
            

            //Products product;
            //using (var repo = new ProductsRepository())
            //{
            //    product = repo.AllIncluding(c => c.OrderDetails).FirstOrDefault(c => c.Id == 1);
            //}

            //product.UnitsInStock = 20;
            //product.State = State.Modified;

            //using (var repo = new ProductsRepository())
            //{
            //    repo.InsertOrUpdate(product);
            //    repo.Save();
            //}
        }
    }
}
